#!/bin/bash

source /home/ckn/layersnap/params
#source /home/ckn/layersnap/actions

case $ACTION in
  status)
    ls_status
  ;;
  snapshot)
    ls_snapshot
  ;;
  restore)
    ls_restore
  ;;
  merge)
    ls_merge
  ;;
esac
