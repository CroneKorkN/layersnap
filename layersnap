#!/bin/bash

source params

case $ACTION in
  status)
    echo status $DIR    
  ;;
  snapshot)
    echo snapshot $DIR
  ;;
  restore)
    echo restore $DIR
  ;;
  merge)
    echo merge $DIR
  ;;
esac
